<%# Hyrax v5.0.1 Override: L#4 uses Account instead of Profile. %>

<% provide :page_header do %>
  <h1><span class="fa fa-id-card" aria-hidden="true"></span> Edit Account</h1>
<% end %>
<div class="card">
  <div class="card-body">
    <div class="col-sm-6 profile">
      <%= render 'edit_primary' %>
    </div>

    <div class="col-sm-6 profile">
      <%= render 'edit_secondary' %>
    </div>
  </div>
</div>
